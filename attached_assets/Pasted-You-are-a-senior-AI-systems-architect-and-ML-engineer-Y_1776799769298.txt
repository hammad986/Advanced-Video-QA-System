You are a senior AI systems architect and ML engineer.

Your task is to UPGRADE the multi-video comparison system with an intelligent gating layer.

---

## 🎯 OBJECTIVE

Make the system:

* Smart (understands when NOT to answer)
* Reliable (no random comparisons)
* Explainable (clear reasoning)

---

## ⚠️ STRICT RULES

* Do NOT modify existing /ask_question
* Do NOT break current pipeline
* Only enhance /compare_videos logic
* All outputs must be deterministic + explainable

---

## 🔥 IMPLEMENT THESE 3 CORE SYSTEMS

---

# 1. TOPIC ALIGNMENT CHECK (CRITICAL)

For each selected video:

* Extract top chunks (title + first chunks)
* Generate embedding (use existing BGE model)

Compute:

* centroid embedding per video
* cosine similarity between videos

---

### Thresholds:

* > = 0.75 → SAME TOPIC
* 0.55–0.75 → PARTIAL OVERLAP
* < 0.55 → DIFFERENT TOPIC

---

# 2. QUERY ALIGNMENT CHECK

* Embed user query
* Compare with each video centroid

If:

* query is not relevant to BOTH videos → mark LOW RELEVANCE

---

# 3. SUFFICIENCY CHECK

For each video:

* Check top-k retrieval scores
* Count relevant chunks

If:

* top score < 0.65 OR chunks < 2
  → mark INSUFFICIENT DATA

---

# 🧠 DECISION ENGINE

Implement:

IF topic_similarity < 0.55:
→ return NOT COMPARABLE

ELIF topic_similarity 0.55–0.75:
→ return PARTIAL COMPARISON

ELIF insufficient data:
→ return INSUFFICIENT CONTENT

ELSE:
→ run normal comparison

---

# 📦 OUTPUT FORMAT (STRICT)

Always return:

{
"status": "COMPARABLE | PARTIAL | NOT_COMPARABLE | INSUFFICIENT",
"reason": "...",
"comparison": "...",
"scores": {
"video_1": 0.87,
"video_2": 0.72
},
"sources": {
"video_1": "[02:10–03:00]",
"video_2": "[01:20–02:00]"
}
}

---

# 🚫 FAILURE CONDITIONS

* Do NOT give answers when topics mismatch
* Do NOT hallucinate comparisons
* Do NOT skip explanation

---

# 🧪 TEST CASES (MANDATORY)

Test with:

1. Same-topic videos → must compare
2. Different-topic videos → must refuse
3. Weak content → must say insufficient

---

# 🚀 FINAL GOAL

System should behave like a HUMAN expert:

* compares when valid
* refuses when invalid
* explains clearly

---

BEGIN IMPLEMENTATION.
